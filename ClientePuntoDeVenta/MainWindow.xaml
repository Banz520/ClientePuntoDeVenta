<Window x:Class="ClientePuntoDeVenta.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Menú Principal - Punto de Venta" 
        Height="720" 
        Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        WindowState="Maximized"
        AllowsTransparency="True"
        >

    <Window.Resources>
        <LinearGradientBrush x:Key="TitleBarBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#252538" Offset="0"/>
            <GradientStop Color="#3A3A5A" Offset="1"/>
        </LinearGradientBrush>

        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="#3A3A5A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#6C5B7B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <Rectangle x:Name="hoverEffect" Fill="#20FFFFFF" Opacity="0" RadiusX="8" RadiusY="8"/>
                                <ContentPresenter x:Name="contentPresenter" 
                                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                                Content="{TemplateBinding Content}"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4A4A6A"/>
                                <Setter Property="BorderBrush" Value="#8D6BB5"/>
                                <Setter TargetName="hoverEffect" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2A2A4A"/>
                                <Setter Property="BorderBrush" Value="#9C27B0"/>
                                <Setter TargetName="hoverEffect" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="WindowControlButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#1E1E2F" CornerRadius="15">
        <Grid>
            <!-- Barra de título personalizada -->
            <DockPanel Background="{StaticResource TitleBarBrush}" Height="40" VerticalAlignment="Top">
                <TextBlock Text="Menú Principal - Punto de Venta" 
                           Foreground="White" 
                           FontSize="16"
                           VerticalAlignment="Center"
                           Margin="15,0"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <Button Style="{StaticResource WindowControlButton}" Click="MinimizeWindow" Background="MediumPurple" Height="auto">
                        <TextBlock Text="_" FontSize="18" Margin="0,-20,0,0"/>
                    </Button>
                    <Button Style="{StaticResource WindowControlButton}" Click="MaximizeWindow" Height="auto">
                        <TextBlock Text="□" FontSize="14" Margin="0,-8,0,0"/>
                    </Button>
                    <Button Style="{StaticResource WindowControlButton}" Click="CloseWindow" Background="MediumVioletRed" Height="auto">
                        <TextBlock Text="×" FontSize="18" Margin="0,-5,0,0" />
                    </Button>
                </StackPanel>
            </DockPanel>

            <!-- Contenido principal -->
            <Grid Margin="0,40,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Encabezado -->
                <StackPanel Grid.Row="0" Margin="20" HorizontalAlignment="Center">
                    <TextBlock Text="Sistema Punto de Venta" 
                               Foreground="White" 
                               FontSize="28"
                               FontWeight="Bold"
                               TextAlignment="Center"/>
                    <TextBlock Text="Desarrollado por Ian Alvarez, Juan Pablo Rivera y Angela Haro" 
                               Foreground="#A0A0C0" 
                               FontSize="16"
                               HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Botones de acción -->
                <WrapPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Style="{StaticResource ModernButton}"
                            Click="BtnInventario_Click">
                        <StackPanel Orientation="Horizontal">
                            
                            <TextBlock Text="Ver Inventario" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource ModernButton}">
                        <StackPanel Orientation="Horizontal">
                            
                            <TextBlock Text="Realizar Venta" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    
                </WrapPanel>
            </Grid>
        </Grid>
    </Border>
</Window>