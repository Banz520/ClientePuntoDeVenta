<Window x:Class="ClientePuntoDeVenta.Vistas.InventarioView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inventario" Height="720" Width="1280"
        Background="#1E1E2F" Foreground="White">
    <Grid Margin="20">
        <TextBlock Text="Inventario Actual" FontSize="24" FontWeight="Bold" Margin="0 0 0 10"/>

        <DataGrid x:Name="InventarioGrid" AutoGenerateColumns="False" Margin="0 40 0 0"
                  Background="#2A2A3D" Foreground="Black" BorderBrush="#444" RowBackground="#333" AlternatingRowBackground="#2A2A3D">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="DarkGray"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="4*"/>
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="*"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding Precio}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>